import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const sum = () => {
  return (
    <View>
      <Text>sum</Text>
    </View>
  );
};

export default sum;

const styles = StyleSheet.create({});
